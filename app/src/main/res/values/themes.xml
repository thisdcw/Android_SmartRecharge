<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Android_SmartRecharge" parent="Base.Theme.Android_SmartRecharge" />
    <style name="Base.Theme.Android_SmartRecharge" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>